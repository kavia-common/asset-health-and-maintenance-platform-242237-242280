[flake8]
max-line-length = 120
extend-ignore =
    # Black-compatible whitespace around slices; harmless if present.
    E203,
    # Line break before binary operator; prefer Black's formatting.
    W503
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    build,
    dist
